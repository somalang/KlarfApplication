<UserControl x:Class="KlarfApplication.View.DefectImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 상단 헤더 -->
            <Border Grid.Row="0" Background="#73009C" Padding="8,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍 Defect Image Viewer"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="13"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentImageInfo}"
                               Foreground="White"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="11"/>
                </StackPanel>
            </Border>

            <!-- 이미지 표시 영역 -->
            <Border Grid.Row="1" Background="#FF1E1E1E">
                <Grid>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  x:Name="ImageScrollViewer">

                        <Grid x:Name="ImageGrid"
                              Background="Transparent"
                              MouseWheel="ImageGrid_MouseWheel"
                              MouseLeftButtonDown="ImageGrid_MouseLeftButtonDown"
                              MouseLeftButtonUp="ImageGrid_MouseLeftButtonUp"
                              MouseMove="ImageGrid_MouseMove"
                              MouseRightButtonDown="ImageGrid_MouseRightButtonDown"
                              MouseRightButtonUp="ImageGrid_MouseRightButtonUp">

                            <Image x:Name="DefectImage"
                                   Source="{Binding DefectImage}"   
                                   Stretch="Uniform" 
                                   RenderOptions.BitmapScalingMode="HighQuality"/>

                            <Canvas x:Name="DrawingCanvas" />
                        </Grid>
                    </ScrollViewer>

                    <TextBlock Text="No Image Loaded"
                               Foreground="Gray"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding NoImageVisibility}"/>
                </Grid>
            </Border>

            <!-- 하단 컨트롤 패널 -->
            <Border Grid.Row="2" Background="#FF2D2D30" Padding="10,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 줌 컨트롤 -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button Content="➕"
                                    Style="{StaticResource ToolButtonStyle}"
                                    Command="{Binding ZoomInCommand}"
                                    ToolTip="Zoom In (Ctrl++)"
                                    Background="Transparent"
                                    Foreground="LightGray"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"/>
                            <Button Content="➖"
                                    Style="{StaticResource ToolButtonStyle}"
                                    Command="{Binding ZoomOutCommand}"
                                    ToolTip="Zoom Out (Ctrl+-)"
                                    Background="Transparent"
                                    Foreground="LightGray"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"/>
                            <Button Content="🔃"
                                    Style="{StaticResource ToolButtonStyle}"
                                    Command="{Binding ResetViewCommand}"
                                    ToolTip="Reset View (Ctrl+0)"
                                    Background="Transparent"
                                    Foreground="LightGray"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"/>
                            <Button Content="🔛"
                                    Style="{StaticResource ToolButtonStyle}"
                                    Command="{Binding FitToWindowCommand}"
                                    ToolTip="Fit to Window"
                                    Background="Transparent"
                                    Foreground="LightGray"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Zoom:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding ZoomLevel, StringFormat={}{0:F0}%}"
                                       Foreground="Cyan"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       MinWidth="50"/>
                        </StackPanel>
                    </Grid>

                    <!-- 밝기 슬라이더 -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Brightness:"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>

                        <Slider Grid.Column="1"
                                x:Name="BrightnessSlider"
                                Minimum="-100"
                                Maximum="100"
                                TickFrequency="10"
                                IsSnapToTickEnabled="True"
                                VerticalAlignment="Center"
                                Value="{Binding Brightness, Mode=TwoWay}"/>

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Brightness, StringFormat={}{0:F0}}"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   TextAlignment="Right"/>
                    </Grid>

                    <!-- 명암 슬라이더 -->
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Contrast:"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>

                        <Slider Grid.Column="1"
                                x:Name="ContrastSlider"
                                Minimum="-100"
                                Maximum="100"
                                TickFrequency="10"
                                IsSnapToTickEnabled="True"
                                VerticalAlignment="Center"
                                Value="{Binding Contrast, Mode=TwoWay}"/>

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Contrast, StringFormat={}{0:F0}}"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   TextAlignment="Right"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
