<UserControl x:Class="KlarfApplication.View.WaferMapViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FF007ACC" Padding="8,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⬢ Wafer Map Viewer"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="13"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding WaferMapStats}"
                               Foreground="White"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="11"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#FF1E1E1E">
                <Viewbox Stretch="Uniform">
                    <Canvas Width="500" Height="500"
                            x:Name="WaferMapCanvas"
                            MouseLeftButtonDown="WaferMapCanvas_MouseLeftButtonDown">
                        <!-- 웨이퍼 맵 -->
                    </Canvas>
                </Viewbox>
            </Border>

            <Border Grid.Row="2" Background="#FF2D2D30" Padding="10,5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                        <Rectangle Width="15" Height="15" Fill="LightGreen" Stroke="White" StrokeThickness="1"/>
                        <TextBlock Text="Good Dies" Foreground="White" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                        <Rectangle Width="15" Height="15" Fill="Red" Stroke="White" StrokeThickness="1"/>
                        <TextBlock Text="Defective Dies" Foreground="White" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="15" Height="15" Fill="DarkGray" Stroke="White" StrokeThickness="1"/>
                        <TextBlock Text="Edge Dies" Foreground="White" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
