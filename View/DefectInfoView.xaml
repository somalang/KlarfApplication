<UserControl x:Class="KlarfApplication.View.DefectInfoViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="LightGray"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3E3E42"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>

    </UserControl.Resources>

    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#9C030D" Padding="8,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊 Defect Information"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="13"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DefectCount, StringFormat=({0} Defects)}"
                               Foreground="White"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="11"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1"
                    Background="#FF252526"
                    Padding="12,8"
                    BorderBrush="#FF3E3E42"
                    BorderThickness="0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="File Name:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentKlarfFile.FileName}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Inspection Date:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentKlarfFile.ResultTimestamp}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Wafer ID:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CurrentKlarfFile.WaferId}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Device ID:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CurrentKlarfFile.InspectionStationId}" Style="{StaticResource InfoValueStyle}"/>

                    <Grid Grid.Row="0" Grid.Column="3" Grid.RowSpan="4" Grid.ColumnSpan="2" 
                          Visibility="{Binding SelectedDieVisibility}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Grid.Column="3" Grid.RowSpan="4" Grid.ColumnSpan="2" 
                          Visibility="{Binding SelectedDieVisibility}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Die Position:" Style="{StaticResource InfoLabelStyle}"/>
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="[" Style="{StaticResource InfoValueStyle}" Margin="10,2,0,2"/>
                                <TextBlock Text="{Binding SelectedDie.Row}" Style="{StaticResource InfoValueStyle}" Margin="0,2"/>
                                <TextBlock Text=", " Style="{StaticResource InfoValueStyle}" Margin="0,2"/>
                                <TextBlock Text="{Binding SelectedDie.Column}" Style="{StaticResource InfoValueStyle}" Margin="0,2"/>
                                <TextBlock Text="]" Style="{StaticResource InfoValueStyle}" Margin="0,2"/>
                            </StackPanel>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Die Type:" Style="{StaticResource InfoLabelStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDie.DieType}" Style="{StaticResource InfoValueStyle}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Defect Count:" Style="{StaticResource InfoLabelStyle}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDie.DefectCount}" Style="{StaticResource InfoValueStyle}"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Show Die:" Style="{StaticResource InfoLabelStyle}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedDie.Show}" Style="{StaticResource InfoValueStyle}"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <DataGrid x:Name="DefectDataGrid"
                          AutoGenerateColumns="False"
                          Background="#FF2D2D30"  
                          Foreground="White"
                          HeadersVisibility="Column"
                          GridLinesVisibility="All"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          BorderThickness="0"
                          ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                          CellStyle="{StaticResource GridCellStyle}"
                          ItemsSource="{Binding Defects}"  
                          SelectionChanged="DefectDataGrid_SelectionChanged"
                          
                          SelectedItem="{Binding SelectedDefect, Mode=TwoWay}">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="50" Binding="{Binding Index}"/>
                        <DataGridTextColumn Header="Defect ID" Width="110" Binding="{Binding DefectId}"/>
                        <DataGridTextColumn Header="X" Width="80" Binding="{Binding XCoord, StringFormat=F2}"/>
                        <DataGridTextColumn Header="Y" Width="80" Binding="{Binding YCoord, StringFormat=F2}"/>
                        <DataGridTextColumn Header="Type" Width="90" Binding="{Binding DefectType}"/>
                        <DataGridTextColumn Header="Size" Width="*" Binding="{Binding Size, StringFormat=F2}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="No Defects" 
                           Foreground="Gray"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding NoDefectsVisibility}"/>
            </Grid>

            <Border Grid.Row="3"
                    Background="#FF252526"
                    Padding="10,8"
                    BorderBrush="#FF3E3E42"
                    BorderThickness="0,1,0,0">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,5">
                        <TextBlock Text="All Dies:" Foreground="LightGreen" VerticalAlignment="Center" FontWeight="Bold" Margin="42,0,10,0"/>
                        <Button Content="|◀◀" Style="{StaticResource NavigationButtonStyle}" Command="{Binding FirstAllDieCommand}" ToolTip="First Die on Wafer" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="◀"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding PreviousAllDieCommand}" ToolTip="Previous Die on Wafer" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Border Background="#FF3E3E42" Padding="5,5" Margin="5,0" CornerRadius="3">
                            <TextBlock Text="{Binding CurrentAllDieInfo}"
                                       Foreground="White"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       MinWidth="70"
                                       TextAlignment="Center"/>
                        </Border>
                        <Button Content="▶"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding NextAllDieCommand}" ToolTip="Next Die on Wafer" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="▶▶|" Style="{StaticResource NavigationButtonStyle}" Command="{Binding LastAllDieCommand}" ToolTip="Last Die on Wafer" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="defects per die:" Foreground="Cyan" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                            <Button Content="|◀◀" Style="{StaticResource NavigationButtonStyle}" Command="{Binding FirstDieCommand}" ToolTip="First Defect in Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Button Content="◀"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding PreviousDieCommand}" ToolTip="Previous Defect in Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Border Background="#FF3E3E42" Padding="10,5" Margin="5,0" CornerRadius="3">
                                <TextBlock Text="{Binding CurrentDieInfo}"
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           MinWidth="60"
                                           TextAlignment="Center"/>
                            </Border>
                            <Button Content="▶"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding NextDieCommand}" ToolTip="Next Defect in Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Button Content="▶▶|" Style="{StaticResource NavigationButtonStyle}" Command="{Binding LastDieCommand}" ToolTip="Last Defect in Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="die with defects:" Foreground="Orange" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                            <Button Content="|◀◀" Style="{StaticResource NavigationButtonStyle}" Command="{Binding FirstDefectCommand}" ToolTip="First Defective Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Button Content="◀"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding PreviousDefectCommand}" ToolTip="Previous Defective Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Border Background="#FF3E3E42" Padding="10,5" Margin="5,0" CornerRadius="3">
                                <TextBlock Text="{Binding CurrentDefectInfo}"
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"
                                           MinWidth="70"
                                           TextAlignment="Center"/>
                            </Border>
                            <Button Content="▶"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding NextDefectCommand}" ToolTip="Next Defective Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                            <Button Content="▶▶|" Style="{StaticResource NavigationButtonStyle}" Command="{Binding LastDefectCommand}" ToolTip="Last Defective Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        </StackPanel>
                    </Grid>

                </Grid>
            </Border>

        </Grid>
    </Border>
</UserControl>