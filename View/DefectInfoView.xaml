<UserControl x:Class="KlarfApplication.View.DefectInfoViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="LightGray"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <!-- DataGrid 헤더/셀 스타일도 로컬 정의 -->
        <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <!-- 헤더 배경도 동일 색 -->
            <Setter Property="Foreground" Value="White"/>
            <!-- 글씨 흰색 -->
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <!-- 셀 배경을 어둡게 -->
            <Setter Property="Foreground" Value="White"/>
            <!-- 글씨 흰색 -->
            <Setter Property="BorderBrush" Value="#FF3E3E42"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>

    </UserControl.Resources>

    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- 헤더 -->
                <RowDefinition Height="Auto"/>
                <!-- 샘플 정보 -->
                <RowDefinition Height="*"/>
                <!-- 불량 리스트 -->
                <RowDefinition Height="Auto"/>
                <!-- 네비게이션 -->
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <Border Grid.Row="0" Background="#9C030D" Padding="8,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊 Defect Information"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="13"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DefectCount, StringFormat=({0} Defects)}"
                               Foreground="White"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="11"/>
                </StackPanel>
            </Border>

            <!-- 샘플 정보 -->
            <Border Grid.Row="1"
                    Background="#FF252526"
                    Padding="12,8"
                    BorderBrush="#FF3E3E42"
                    BorderThickness="0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="File Name:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FileName}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Inspection Date:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding InspectionDate}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Wafer ID:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding WaferId}" Style="{StaticResource InfoValueStyle}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Device ID:" Style="{StaticResource InfoLabelStyle}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DeviceId}" Style="{StaticResource InfoValueStyle}"/>
                </Grid>
            </Border>

            <!-- 불량 리스트 -->
            <Grid Grid.Row="2">
                <DataGrid x:Name="DefectDataGrid"
          AutoGenerateColumns="False"
          Background="#FF2D2D30"  
                    Foreground="White"
                    HeadersVisibility="Column"
          GridLinesVisibility="All"
          CanUserAddRows="False"
          SelectionMode="Single"
          BorderThickness="0"
          ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
          CellStyle="{StaticResource GridCellStyle}"
          ItemsSource="{Binding Defects}"  
          SelectionChanged="DefectDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Width="50" Binding="{Binding Index}"/>
                        <DataGridTextColumn Header="Defect ID" Width="110" Binding="{Binding DefectId}"/>
                        <DataGridTextColumn Header="X" Width="80" Binding="{Binding XCoord, StringFormat=F2}"/>
                        <DataGridTextColumn Header="Y" Width="80" Binding="{Binding YCoord, StringFormat=F2}"/>
                        <DataGridTextColumn Header="Type" Width="90" Binding="{Binding DefectType}"/>
                        <DataGridTextColumn Header="Size" Width="*" Binding="{Binding Size, StringFormat=F2}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="No Defects" 
               Foreground="Gray"
               FontSize="16"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Visibility="{Binding NoDefectsVisibility}"/>
            </Grid>
            <!-- 네비게이션 -->
            <Border Grid.Row="3"
                    Background="#FF252526"
                    Padding="10,8"
                    BorderBrush="#FF3E3E42"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Die 네비게이션 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="Die:" Foreground="Cyan" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                        <Button Content="|◀◀" Style="{StaticResource NavigationButtonStyle}" Command="{Binding FirstDieCommand}" ToolTip="First Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="◀"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding PreviousDieCommand}" ToolTip="Previous Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Border Background="#FF3E3E42" Padding="10,5" Margin="5,0" CornerRadius="3">
                            <TextBlock Text="{Binding CurrentDieInfo}"
                                       Foreground="White"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       MinWidth="60"
                                       TextAlignment="Center"/>
                        </Border>
                        <Button Content="▶"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding NextDieCommand}" ToolTip="Next Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="▶▶|"  Style="{StaticResource NavigationButtonStyle}" Command="{Binding LastDieCommand}" ToolTip="Last Die" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                    </StackPanel>

                    <!-- Defect 네비게이션 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Defect:" Foreground="Orange" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                        <Button Content="|◀◀" Style="{StaticResource NavigationButtonStyle}" Command="{Binding FirstDefectCommand}" ToolTip="First Defect" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="◀"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding PreviousDefectCommand}" ToolTip="Previous Defect" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Border Background="#FF3E3E42" Padding="10,5" Margin="5,0" CornerRadius="3">
                            <TextBlock Text="{Binding CurrentDefectInfo}"
                                       Foreground="White"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       MinWidth="70"
                                       TextAlignment="Center"/>
                        </Border>
                        <Button Content="▶"    Style="{StaticResource NavigationButtonStyle}" Command="{Binding NextDefectCommand}" ToolTip="Next Defect" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                        <Button Content="▶▶|"  Style="{StaticResource NavigationButtonStyle}" Command="{Binding LastDefectCommand}" ToolTip="Last Defect" BorderBrush="Transparent" BorderThickness="0" Foreground ="LightGray" Background="Transparent"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
