<UserControl x:Class="KlarfApplication.View.FileListViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FF0E7A0D" Padding="8,5">
                <TextBlock Text="📁 File List Viewer" Foreground="White" FontWeight="Bold" FontSize="13"/>
            </Border>

            <ToolBar Grid.Row="1" Background="#FF3E3E42" BorderBrush="#FF555555" BorderThickness="0,1">
                <Button Content="📂 Open" Foreground="White" Command="{Binding OpenFileCommand}" ToolTip="Open Klarf File (Ctrl+O)" Padding="8,4"/>
                <Separator/>
                <Button Content="🖼️ Images" Foreground="White" Command="{Binding OpenImageFolderCommand}" ToolTip="Open Image Folder" Padding="8,4"/>
                <Separator/>
                <Button Content="🔄 Refresh" Foreground="White" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" ToolTip="Refresh (F5)" Padding="8,4"/>
                <Separator/>
                <Button Content="🗑️ Clear" Foreground="White" Command="{Binding ClearFilesCommand}" HorizontalAlignment="Right" ToolTip="Clear All Files" Padding="8,4"/>
            </ToolBar>

            <Grid Grid.Row="2">
                <TreeView Background="#FF2D2D30"
                          Foreground="White"
                          BorderThickness="0"
                          x:Name="FileTreeView"
                          ItemsSource="{Binding TreeNodes}"
                          SelectedItemChanged="FileTreeView_SelectedItemChanged">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="2"/>
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Header}" Foreground="White"/>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>

                <TextBlock Text="No Files" 
                           Foreground="Gray"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding NoFilesVisibility}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>