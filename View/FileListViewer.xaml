<UserControl x:Class="KlarfApplication.View.FileListViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="#FF3E3E42" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FF0E7A0D" Padding="8,5">
                <TextBlock Text="📁 File List Viewer" Foreground="White" FontWeight="Bold" FontSize="13"/>
            </Border>

            <ToolBar Grid.Row="1" Background="#FF3E3E42" BorderBrush="#FF555555" BorderThickness="0,1">
                <Button Content="📂 Open" Command="{Binding OpenFileCommand}" ToolTip="Open Klarf File (Ctrl+O)" Padding="8,4"/>
                <Separator/>
                <Button Content="🖼️ Images" Command="{Binding OpenImageFolderCommand}" ToolTip="Open Image Folder" Padding="8,4"/>
                <Separator/>
                <Button Content="🔄" Command="{Binding RefreshCommand}" ToolTip="Refresh (F5)" Padding="8,4"/>
                <Separator/>
                <Button Content="🗑️ Clear" Command="{Binding ClearFilesCommand}" ToolTip="Clear All Files" Padding="8,4"/>
            </ToolBar>

            <TreeView Grid.Row="2"
                      Background="#FF2D2D30"
                      Foreground="White"
                      BorderThickness="0"
                      x:Name="FileTreeView"
                      SelectedItemChanged="FileTreeView_SelectedItemChanged">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Padding" Value="2"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </Grid>
    </Border>
</UserControl>
